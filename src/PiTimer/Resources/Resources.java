package PiTimer.Resources;

import javax.swing.*;
import java.nio.file.Path;

public class Resources {
    public final static ImageIcon arrowDown=new ImageIcon(Resources.class.getResource("arrowDown.png")),
            arrowUp=new ImageIcon(Resources.class.getResource("arrowUp.png")),
            logo=new ImageIcon(Resources.class.getResource("logo.png"));
}
